---
description: 
---

### Workflow: Create Microservice

1. Create new folder in `/backend`
2. Initialize framework project (Spring Boot / Laravel / Django)
3. Add standard folders:
   controllers
   services
   repositories
   entities
   dto
   mappers
4. Add `.env.example`
5. Register service in `docker-compose.yml`
6. Add README.md