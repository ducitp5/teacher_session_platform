---
description: 
---

### Workflow: CRUD Resource

1. Create database table
2. Create entity
3. Create repository
4. Create service
5. Create controller
6. Create endpoints:

GET /api/resource
GET /api/resource/{id}
POST /api/resource
PUT /api/resource/{id}
DELETE /api/resource/{id}

7. Create frontend list page
8. Create frontend form